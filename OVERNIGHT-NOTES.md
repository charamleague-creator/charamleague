# บันทึกงานตอนกลางคืน (ผู้ใช้ให้ทำต่อได้เลยจนถึงเช้า)

อ่านไฟล์นี้ก่อนอันแรกตอนกลับมาครับ — สรุปทุกจุดที่ผมต้อง**ตัดสินใจเองโดยไม่ได้ถาม**เพราะตอนนั้นไม่มีคุณอยู่ ทุกจุดมีเหตุผลกำกับไว้ ถ้าอันไหนไม่ตรงกับที่คิดไว้ บอกได้เลยจะแก้ให้

สถานะ ณ ตอนที่เขียนไฟล์นี้: กำลังเริ่ม Phase 2.2 ต่อจาก Phase 2.1 ที่ push ไปแล้ว (`4f9c6fb`)

---

## งานที่ทำ (จะอัปเดตทุกครั้งที่จบ sub-phase)

### Phase 2.2 — ฉีกสัญญา (release) + ป้องกัน ping-pong ✅
- เพิ่ม `freeAgents` และ `releaseLog` collection ใต้ league
- `releasePlayer()`: ลบผู้เล่นจากทีม → สร้าง free agent (คง playerId เดิม) → log การฉีกสัญญาด้วย deterministic ID `${season}_${teamId}_${playerId}` (ถ้ามีอยู่แล้ว = บล็อกซ้ำ)
- Manager คุมทีมตัวเองฉีกสัญญาได้เอง (ไม่ต้องรอ admin) ผ่าน rule `managesTeam()` ที่ query `get()` เช็ค `managerUid`
- **บั๊กที่เจอจาก rules test แล้วแก้ได้จริง:** ฟังก์ชัน rules ที่นิยามไว้นอก `service cloud.firestore` (top-level) **ไม่เห็น** ตัวแปร `database` จาก path capture โดยอัตโนมัติ ต้องส่งเป็น parameter เข้าไปเอง (`managesTeam(database, leagueId, teamId)`) ไม่งั้น `get()` จะพังแบบ "Null value error" แบบไม่มี error message ที่ชัดเจน — เป็นตัวอย่างที่ดีว่าทำไมต้องรัน rules test จริงเสมอ ไม่ใช่แค่เขียนแล้วเชื่อว่าถูก
- ทดสอบ manual ผ่าน emulator ครบ รวมถึงจำลอง ping-pong (ใช้ firebase-admin เขียนผู้เล่นตัวเดิม ID เดิมกลับเข้าทีม แล้วลองฉีกซ้ำ) → บล็อกถูกต้องด้วยข้อความ error ตรงเป้า

### Phase 2.3 — ขายตรงระหว่างทีม + โควตา ✅
- เพิ่ม `saleOffers` (เสนอราคา) และ `transfers` (ประวัติที่โอนสำเร็จแล้ว ใช้คำนวณโควตาสด) ใต้ league
- `src/features/leagues/quotas.ts` — pure function `canSell`/`canReceive` (โควตาขายผ่านประมูล ≤4, ขายรวม ≤5, รับเข้า ≤5 ต่อฤดูกาล ตามเอกสารข้อ 5.2 เป๊ะๆ) มี unit test ครบ
- Flow: ทีมใดทีมหนึ่ง (ผู้ขายหรือผู้ซื้อ) เสนอราคา → อีกฝ่ายกด "ยอมรับ" (แก้ได้แค่ field `status`) → **แอดมินกด "ปิดการขาย" เป็นคนย้ายผู้เล่นจริง** (เช็คโควตาก่อนย้ายเสมอ ตาม guideline #5) → บันทึก `transfers` (คง playerId เดิมข้ามทีม เหมือน release)
- ทดสอบ manual ผ่าน emulator ครบ 3 ฝั่ง (ผู้ขาย เสนอ → ผู้ซื้อ ยอมรับ → แอดมิน ปิดการขาย) เช็ค Firestore ตรงว่าผู้เล่นย้ายทีมจริงพร้อม ID เดิม และ transfer record ถูกต้อง

### Phase 2.4 — ประมูล ✅
- เพิ่ม `auctionListings` collection: ทีมขายส่งเข้าคิว (`pending_approval`) → **แอดมินอนุมัติ** (ตอนนี้เองที่นับเข้าโควตาแล้ว แม้ยังไม่ปิดประมูล ตามเอกสารข้อ 5.2 ตรงตัว) → เปิดประมูล (`open`) manager ทีมอื่นประมูลได้เอง (ห้ามทีมขายประมูลนักเตะตัวเอง) → **แอดมินปิดประมูล** กำหนดผู้ชนะ/ย้ายผู้เล่นจริง
- ขยาย `quotas.ts`'s `canSell()` ให้รับ `pendingSameTypeCount` (จำนวนรายการประมูลที่เปิดอยู่แต่ยังไม่ปิดของทีมนั้น) มาบวกรวมกับ transfers ที่ปิดแล้ว ตรงตามข้อกำหนด "ยืนยันแล้วแต่ยังไม่ปิดประมูลต้องนับโควตาด้วย"
- **ใช้ Firestore `runTransaction` สำหรับการประมูล** (`placeBid`) กัน race condition ตอนมีคนประมูลพร้อมกันสองคน (ตาม data-modeling-guidelines ข้อ 3) — ฝั่ง rules ก็เช็คซ้ำว่าราคาใหม่ต้องสูงกว่าราคาปัจจุบันจริง (defense in depth)
- **บั๊กที่เจอจาก rules test แล้วแก้ได้จริง (เป็น test ผมเขียนผิดเอง ไม่ใช่ rules ผิด):** เทสเดิมคาดหวังว่าประมูล "เท่าราคาเริ่ม" ต้องถูกปฏิเสธ แต่จริงๆ ตามธรรมชาติการประมูล บิดแรกควรเปิดได้ที่ราคาตั้งต้นพอดี (>= ไม่ใช่ >) แก้ assertion ให้ตรงกับ business rule ที่ถูกต้องแทนที่จะแก้ rule ให้ตรง test ที่เขียนผิด
- ทดสอบ manual ผ่าน emulator ครบ: ส่งประมูล → อนุมัติ → ประมูลแข่งกัน → ปิดประมูล (มีผู้ชนะ, ย้ายผู้เล่นจริง+บันทึก transfer ถูกต้อง) และเคส "ปิดประมูลไม่มีคนประมูลเลย" (`closed_no_winner`, ผู้เล่นไม่ถูกย้าย)
- **จุดที่ยังไม่ทำ (ถ้าเจอในทางปฏิบัติจริงต้องมาคุยกัน):** ถ้าผู้ชนะประมูล (ราคาสูงสุด) ดันเกินโควตารับเข้าไปแล้วตอนปิดประมูล ระบบแค่ throw error บล็อกการปิด ไม่ auto-fallback ไปเลือกผู้เสนอราคาอันดับสองให้ — แอดมินต้องจัดการเอง (ยังไม่มีปุ่ม "ตัดสิทธิ์ผู้ประมูลคนนี้" ในเวอร์ชันนี้)

### Phase 2.5/2.6 — Academy72 + คูปองส่วนลด: **หยุดไว้ก่อน ยังไม่ทำ** ⏸️
หยุดสองอันนี้ไว้ ข้ามไปทำ Phase 3 (บอลถ้วย) ต่อแทน เพราะทั้งสองอันพึ่งพาข้อมูลที่เอกสารต้นฉบับไม่ได้ระบุไว้ชัด และเดาต่อไปมีความเสี่ยงสูงที่จะต้องรื้อทำใหม่:
- **Academy72** ("การ์ดสุ่มผู้เล่น"): ไม่รู้ว่า "เปิดการ์ด" ได้ผู้เล่นแบบสุ่มจากพูลไหน (ไม่มีฐานข้อมูลนักเตะกลางในระบบ), ไม่รู้ราคา, และกลไก "บล็อกตอนส่ง Lineup ถ้าเกิน" ต้องมีระบบ "ส่ง Lineup" (ยืนยันผู้เล่น 18 คนต่อฤดูกาล) อยู่ก่อน ซึ่งยังไม่มีในระบบเลย (Phase 1 ตัดสินใจเลื่อนไว้ตั้งแต่ตอนนั้น)
- **คูปองส่วนลด**: ใช้ลดราคาตอนซื้อ/เปิดการ์ด แต่ยังไม่มีระบบเงิน/งบประมาณทีมให้ลด (ข้อ 1 ด้านล่าง) และผูกกับ Academy72 ที่ก็ยังสร้างไม่ได้ด้วยเหตุผลเดียวกัน

**สรุป: สองอันนี้รอคุยกับคุณตอนเช้าก่อนแน่นอน** — ไม่ใช่แค่ "ทำแบบเดาไปก่อน" เพราะความเสี่ยงสร้างผิดสูงเกินไป

### Phase 3 — บอลถ้วย (แพ้คัดออก, ยังไม่รวมบอลโลก) ✅
- สร้างฟีเจอร์ใหม่ `src/features/cups/` — `bracket.ts` เป็น pure function ล้วน (generateBracket, propagateWinner, updateCupMatchResult) แยกจาก Firestore เพื่อเทสได้ตรงไปตรงมา มี unit test 12 เคสรวม cascade หลายชั้น
- **แก้บั๊กจริงตั้งแต่ตอนเขียน (ก่อนแม้จะรัน test):** ถ้า pad ทีมด้วย null แล้วจับคู่ตรงๆ ตามลำดับ (เช่น 5 ทีม → เติม null 3 ตัวต่อท้ายเป็น 8) จะเกิด "บายชนกับบาย" ได้ (คู่ที่เป็น null-vs-null ไม่มีทางตัดสินผล ทำให้รอบถัดไปค้างตลอดกาล) แก้โดยแจกบายให้แยกคนละนัดกันก่อน แล้วค่อยจับคู่ทีมจริงที่เหลือ — มี unit test คุมเคสนี้ไว้แล้ว (5 ทีม, 3 บาย ไม่ชนกันเอง)
- Bracket ทั้งชุด (ทุกรอบ) สร้างพร้อมกันตอน `createCup` เป็น placeholder ('pending') รอผลรอบก่อนหน้าไล่เติมเข้ามาเรื่อยๆ ผ่าน `propagateWinner`
- **cascade reset**: แก้ผลนัดที่ตัดสินไปแล้วให้ผู้ชนะเปลี่ยน จะไล่ล้างทุกรอบถัดไปที่เคย "สร้างขึ้นจากผู้ชนะเดิม" แบบ recursive (ไม่ใช่แค่ชั้นถัดไปชั้นเดียว) — ตรงตามข้อกำหนด "แอดมินแก้ทีมที่เข้ารอบย้อนหลังได้ พร้อม cascade รีเซ็ต" เทสยืนยันด้วย bracket 8 ทีมที่ไล่ล้างทะลุ 2 ชั้น (รอบ2 + รอบชิง) จากการแก้ผลรอบ 1
- **รายงาน/แก้ผลนัดบอลถ้วยเป็นสิทธิ์ admin ทั้งหมดเสมอ** (ต่างจากนัดลีกที่ manager รายงานเองได้) เพราะการ propagate ผู้ชนะเข้ารอบถัดไปต้องเขียนหลาย document ข้ามคู่ทีมที่ไม่เกี่ยวกับผู้รายงานเลย (เช่นรอบชิง) — ปัญหาเดียวกับที่เจอตอนขายย่อย/ประมูล จึงแก้ด้วยวิธีเดียวกัน (ให้ admin เป็นคนกดจริงเสมอ)
- ทดสอบ manual ผ่าน emulator ครบ: สร้างถ้วย 4 ทีม → รายงานผลรอบ 1 ทั้งคู่ (ส่งเข้ารอบชิงถูกทีม) → รายงานผลรอบชิง (สถานะถ้วยเปลี่ยนเป็น "จบแล้ว") → **แก้ผลรอบ 1 ย้อนหลัง (พลิกผู้ชนะ) → รอบชิงถูกล้างกลับเป็นค่าว่างถูกต้อง + สถานะถ้วยกลับเป็น "กำลังแข่ง" อัตโนมัติ** ตรงตามที่ออกแบบเป๊ะ
- **ยังไม่ทำ (ตั้งใจเลื่อนไว้ ไม่ใช่ลืม):** บอลโลก (จับคู่ manager แบบสุ่ม + สุ่มทีมชาติ) — มีโครงสร้างต่างจากถ้วยใหญ่/เล็กมาก (จับคู่คนไม่ใช่ทีม, ต้องมี "รายชื่อทีมชาติ" ที่เอกสารไม่ได้ระบุมา) และเงินรางวัลตอนจบฤดูกาล (ต้องรอระบบเงินเหมือนกัน)

### Phase 5 — หอเกียรติยศ ✅
- Top-level collection `hallOfFame/{entryId}` (ไม่ผูกกับลีกใดลีกหนึ่ง เพราะแชมป์ผูกกับ "คน" ข้ามทั้งระบบ) เก็บ `managerUid`/`managerName`/`teamName` เป็น **snapshot ตายตัว** ตอนบันทึก ไม่ derive จากทีมปัจจุบันย้อนหลัง (ตรงตามข้อกำหนด 5.5 และ guideline #6 เป๊ะๆ — นี่คือบั๊กเดิมที่เอกสารเตือนไว้ตรงๆ "ผู้จัดการทีมที่ย้ายทีมเสียประวัติ")
- แก้ไม่ได้เลยแม้เป็น admin (`allow update: if false`) — ผิดให้ลบสร้างใหม่ กันไม่ให้ snapshot ประวัติถูกแก้แบบเงียบๆ ทีหลัง
- ฟอร์มกรอกย้อนหลัง: เลือกลีก+ทีมจากดรอปดาวน์ (ดึง managerUid/managerName ปัจจุบันมาอัตโนมัติ) แต่ยังพิมพ์เอง (ทีม/manager) ได้เสมอ เผื่อกรอกแชมป์เก่าก่อนระบบมีอยู่ที่ทีมนั้นอาจไม่มีในระบบแล้ว — ตรงตามที่เอกสารขอ
- pure function `isFirstChampionshipForManager()` (เช็คก่อนบันทึกเสมอ ตาม guideline #5) — ตรวจแล้วว่านับแยกทีละประเภทถูกต้อง มี unit test ครบ, ทดสอบ manual เห็น 🌟 ตอนแชมป์ครั้งแรกจริง และไม่เห็นตอนแชมป์ครั้งที่ 2 ของ manager+ประเภทเดียวกัน
- **ยังไม่ทำ:** การแจกคูปองจริงตอนเป็นแชมป์ครั้งแรก (แค่ detect + แสดงผลใน UI) เพราะรอระบบเงิน/คูปองที่ยังไม่ได้ตกลงกัน (Phase 2.5/2.6 — ดูข้อ 1) — ข้อมูลที่เก็บไว้ตรงนี้พร้อมให้ Phase 4 (คูปองรางวัลแชมป์) มาต่อได้ทันทีเมื่อคุยเรื่องเงินจบแล้ว
- **หมวดหมู่แชมป์ที่รองรับ:** ลีกสูงสุด/ลีกรอง/ถ้วยใหญ่/ถ้วยเล็ก/บอลโลก — 5 ประเภทตามเอกสาร แต่ระบบ League ยังไม่มี field "tier" (สูงสุด/รอง) จริงๆ ตอนกรอกแอดมินต้องเลือกหมวดหมู่ถูกต้องเอง ระบบไม่ได้ตรวจสอบว่าลีกที่เลือกจริงๆเป็นลีกสูงสุดหรือรองในทางเทคนิค

### Phase 6 (บางส่วน) — Log กิจกรรมแอดมิน ✅
- top-level collection `adminActivityLog/{logId}` — อ่านได้เฉพาะ admin (ไม่ public เพราะเผยรายละเอียดปฏิบัติการ), เขียนได้เฉพาะ admin และต้อง log การกระทำของ**ตัวเอง**เท่านั้น (`actorUid == request.auth.uid` กันปลอมว่าคนอื่นทำ), แก้/ลบไม่ได้เด็ดขาด (audit log แก้ได้ก็ไม่มีประโยชน์)
- ใช้ `serverTimestamp()` เสมอ (กัน client ปลอมเวลา) และผูก log write เข้ากับ **batch เดียวกัน** ของ action หลักเสมอ (ไม่ใช่ write แยก) เพื่อไม่ให้ log กับ action จริงไม่ตรงกันถ้า write ใดล้มเหลว
- Retrofit เข้า admin action ที่กระทบข้อมูลมากที่สุดก่อน (ไม่ได้ทำทุกจุดคืนนี้ — ดูข้อ 10): `create_league`, `delete_league`, `end_season`, `start_new_season`, `set_team_forfeited`, `complete_sale`, `approve/reject/close_auction`, `create_cup`, `report/edit_cup_match_result` (แยก log คนละชื่อ action ระหว่างรายงานผลครั้งแรก vs แก้ผลย้อนหลัง — เคสหลังนี้สำคัญกว่าเพราะกระทบ cascade), `add/remove_hall_of_fame_entry`
- **เจอ+แก้จุดเสี่ยงตั้งแต่ตอนออกแบบ (ก่อนรัน test):** เพราะ `writeBatch` เป็น atomic ถ้า log write ตัวเดียวถูก rules ปฏิเสธ (เช่น `auth.currentUser` เป็น null ซึ่งไม่ควรเกิดแต่ป้องกันไว้) จะฉุดให้ action หลักที่สำคัญกว่าพังไปด้วย ป้องกันโดยให้ `buildCurrentAdminLogWrite` คืนฟังก์ชัน no-op เงียบๆถ้าไม่มี currentUser แทนที่จะยัด write ที่จะพังเข้าไปในนั้น
- **เจอบั๊กเล็กๆตอนทดสอบจริง (ไม่ใช่บั๊ก rules):** หน้า log เดิม subscribe อ่าน `adminActivityLog` ทันทีตอน mount ไม่รอเช็ค role ก่อน ทำให้ manager ที่เข้าหน้านี้ (ก่อนถูก redirect) เปิด listener ที่โดน permission-denied ค้างไว้ แก้โดยเช็ค `role === 'admin'` ก่อนเริ่ม subscribe ในตัว useEffect เอง (ไม่ subscribe เลยถ้าไม่ใช่ admin) สะอาดกว่าเดิม
- รันเทสทั้งชุดซ้ำหลัง retrofit (62 rules tests, 38 unit tests) ผ่านหมดยืนยันว่าไม่มี regression จากการแก้ไฟล์เดิมหลายจุด — ทดสอบ manual จริงด้วย (สร้างลีก + เริ่มฤดูกาลใหม่ แล้วเช็คว่า log ขึ้นถูกต้องพร้อม timestamp จริง)
- **ยังไม่ทำ (ตั้งใจ ไม่ใช่ลืม):** ยังไม่ retrofit ทุก admin action ที่มีในระบบ (เช่น `releasePlayer`ที่ manager ก็ทำได้เอง, `createTeam`, `addPlayer`/`removePlayer`, `respondToSaleOffer`, `createSaleOffer`, `createAuctionListing`, `placeBid`) — เลือกทำจุดที่ "แอดมินเป็นคนกดจริง และกระทบข้อมูลวงกว้าง" ก่อน ถ้าต้องการ log ให้ครบทุก action (รวม manager actions ด้วย) ทำเพิ่มได้ไม่ยาก แค่เพิ่ม `buildCurrentAdminLogWrite(...)` เข้า writes array ของฟังก์ชันนั้นๆ
- **ยังไม่ทำ:** backup/restore อัตโนมัติ (เอกสารข้อ 5.6 อีกข้อ) — ไม่ได้แตะเรื่องนี้เลยเพราะต้องสร้างทรัพยากรระดับ GCP (bucket, Cloud Scheduler) ซึ่งมีผลเรื่องค่าใช้จ่าย/สิทธิ์เข้าถึงที่ควรถามคุณก่อนสร้างจริง ไม่ใช่แค่โค้ดแอป

## ข้อสมมติ/จุดที่ต้องตัดสินใจเอง (ทบทวนตอนเช้า)

10. **Log กิจกรรมแอดมินยังไม่ครบทุก action** — ดูรายละเอียดในหัวข้อ Phase 6 ด้านบน ถ้าต้องการให้ครอบคลุมทุกจุด (รวม manager self-service actions ด้วยหรือไม่) บอกได้เลย จะเพิ่มให้
8. **หอเกียรติยศเป็นระบบกรอกด้วยมือทั้งหมด ไม่ auto-capture ตอนจบฤดูกาล/ปิดถ้วย** — เอกสารข้อ 5.5 พูดถึงการ "กรอกย้อนหลัง" เป็นหลัก (ของระบบเดิมก็ไม่มี auto-capture) เลยตีความว่าเป็นฟีเจอร์กรอกมือทั้งหมด ถ้าอยากให้ระบบสร้าง entry ให้อัตโนมัติทุกครั้งที่ End Season/ปิดถ้วยชิงจบ (ไม่ต้องกรอกมือ) ทำได้ไม่ยาก แต่ต้องตัดสินใจว่าลีกไหนคือ "ลีกสูงสุด" vs "ลีกรอง" ก่อน (ข้อ 9)
9. **ยังไม่มีทาง mark ว่าลีกไหนเป็น "ลีกสูงสุด" vs "ลีกรอง" ในระบบจริงๆ** — ตอนนี้แค่เป็น label ที่แอดมินเลือกตอนกรอกหอเกียรติยศเอง ถ้าจะทำ auto-capture (ข้อ 8) หรือจะให้ Phase 4 (คูปองรางวัล) รู้เองว่าใครคือแชมป์ลีกไหน ต้องเพิ่ม field "tier" ให้ League ก่อน
6. **บอลถ้วยรายงาน/แก้ผลเป็นสิทธิ์ admin ทั้งหมด ไม่ให้ manager รายงานเองแม้แต่รอบแรก** — ต่างจากนัดลีกที่ manager รายงานเองได้ เหตุผลเดียวกับข้อ 4 (ข้ามสิทธิ์ 2 ทีมไม่ได้ปลอดภัยด้วย rules ล้วน) ถ้าอยากให้ manager รายงานผลรอบแรกได้เอง (ไม่ต้องรอ admin) ต้องคุยเรื่อง Cloud Function เหมือนข้อ 4
7. **ยังไม่มีระบบเลือก/สุ่มลำดับทีมก่อนสร้าง bracket** — ตอนนี้แอดมินติ๊กเลือกทีมแล้วจับคู่ตามลำดับที่ติ๊ก (ไม่สุ่ม ไม่ seed ตามอันดับ) ถ้าต้องการ seed ตามผลงานฤดูกาลก่อนหรือสุ่มจริง ต้องเพิ่มทีหลัง

4. **ทำไมขายย่อยต้องผ่านแอดมินกดปิดการขาย ทั้งที่ทีมสองทีมตกลงกันเองแล้ว:** ติด Firestore Security Rules — การย้ายผู้เล่นต้อง "ลบจากทีม A" พร้อม "สร้างในทีม B" ในการเขียนเดียวกัน แต่กฎ rules เช็คทีละ document ไม่มีทางให้ manager ของทีม A เขียนสิทธิ์ข้ามไปสร้างเอกสารในทีม B ได้อย่างปลอดภัยโดยไม่เปิดช่องโหว่ (นอกจากใช้ Cloud Function ที่คุมทั้ง 2 ฝั่งได้ ซึ่งตัดสินใจไม่ใช้ตั้งแต่ Phase 0) เลยให้ทั้งสองทีมตกลงกันในระบบก่อน (เสนอ→ยอมรับ) แล้วแอดมินเป็นคน "กดจริง" ครั้งเดียว เหมือนบทบาทแอดมินตอนปิดประมูลที่เอกสารกำหนดไว้อยู่แล้ว — ถ้าไม่ชอบ flow นี้ (อยากให้ manager ทำเองจบในตัวได้เลย) ต้องคุยกันเรื่องใช้ Cloud Function เพิ่มงบ hosting/ฟังก์ชัน
5. **ราคาขาย (price) เป็นตัวเลขที่สองทีมตกลงกันเอง ไม่ใช่ราคาคงที่จากระบบ** — เก็บไว้ในประวัติ transfer เฉยๆ ยังไม่มีผลกับเงิน/งบประมาณทีมใดๆ (ตามข้อ 1)

1. **เรื่องเงิน/งบประมาณทีม (budget) ยังไม่มีในระบบเลย** — เอกสารบทเรียนพูดถึง "เงินรางวัล" และ "ทีมใช้เงินไปฉีกสัญญา/ซื้อนักเตะ" แต่ไม่ได้ระบุตัวเลขเริ่มต้น/สูตรราคาที่แน่ชัด ผมเลือก **ยังไม่ทำระบบเงินตอนนี้** — ฉีกสัญญาไม่มีผลต่อเงินใดๆ ในเวอร์ชันนี้ รอจนกว่าจะถึง sub-phase ที่ราคาซื้อขายจริงๆเข้ามาเกี่ยว (ขายตรง 2.3 / ประมูล 2.4) ซึ่งราคาซื้อขายทีมกำหนดกันเองอยู่แล้ว (ไม่ใช่ตัวเลขคงที่จากระบบ) เลยไม่จำเป็นต้องมีงบประมาณตายตัวก็ยังพอสร้างระบบซื้อ-ขายพื้นฐานได้ — **ต้องคุยกันจริงจังว่าจะมีระบบเงิน/งบประมาณทีมไหม และถ้ามีเริ่มต้นเท่าไหร่** ก่อนจะทำ Academy72 (2.5) และคูปอง (2.6) เพราะสองอันนี้น่าจะพึ่งพาราคา/เงินที่ตายตัวกว่า
2. **ใครฉีกสัญญาได้:** ให้ manager เจ้าของทีมทำเองได้เลย ไม่ต้องรอ admin อนุมัติ (ต่างจากประมูลที่เอกสารบอกชัดว่า admin ต้องอนุมัติ) เพราะเอกสารไม่ได้ระบุว่าฉีกสัญญาต้องขอ admin อนุมัติ ถ้าไม่ตรงกับที่คิดไว้บอกได้เลย
3. **สถานะลีกที่ใช้เปิด market จริงๆ คือ "กำลังแข่งขัน" (in_season) ไม่ใช่ "ตลาดเปิด" (transfer_window)** — ตามตัวหนังสือเอกสารข้อ 5.2 ("เสนอซื้อ-ขายนักเตะระหว่างทีมได้เฉพาะตอนลีก 'กำลังแข่งขัน' เท่านั้น") ชื่อ status สลับความหมายจากที่คนทั่วไปจะเดา (คำว่า "ตลาดเปิด" ฟังดูน่าจะเปิดให้ซื้อขายได้ แต่จริงๆคือปิด) — ผมทำตามตัวหนังสือเอกสารเป๊ะๆ ไม่ได้เดาเอง แต่อยากให้ช่วย double-check ว่าเข้าใจตรงกันไหม เพราะถ้าผิดจะกระทบทุก sub-phase ถัดไปด้วย
