# บันทึกงานตอนกลางคืน — สรุปสำหรับตอนเช้า

## TL;DR

ทำต่อจาก Phase 2.1 (squad foundation) ที่ push ไปก่อนคุณเข้านอน จนถึง **Phase 6 บางส่วน** — commit ทุกอันแยกกันตาม sub-phase, push ขึ้น GitHub ทุกครั้ง (CI/CD deploy อัตโนมัติ), เช็คแล้วว่าเว็บจริง (`charamleague-abbb8.web.app`) ยังทำงานปกติหลัง push ทั้งหมด

**Commit ที่เพิ่มเข้ามาคืนนี้ (เรียงตามลำดับ):**
1. `45a304c` — Phase 2.2: ฉีกสัญญา + ป้องกัน ping-pong
2. `59ac209` — Phase 2.3: ขายตรงระหว่างทีม + โควตา
3. `dd436f9` — Phase 2.4: ประมูล
4. `c5fcec6` — Phase 3: บอลถ้วย (bracket + cascade reset)
5. `8575a60` — Phase 5: หอเกียรติยศ
6. `bd78736` — Phase 6 (บางส่วน): log กิจกรรมแอดมิน

ทุก commit ผ่าน build + unit test + Firestore rules test (ผ่าน emulator จริง) + ทดสอบ manual ผ่าน browser จริงก่อน push ทุกครั้ง ไม่มีจุดไหนข้ามการทดสอบ

**สิ่งที่ "หยุดไว้ก่อน" ไม่ได้ทำ เพราะต้องคุยกับคุณก่อนแน่ๆ (ไม่ใช่แค่เดาแล้วเสี่ยงทำผิด):**
- **Phase 2.5 (Academy72) + Phase 2.6 (คูปองส่วนลด)** — ต้องมีระบบเงิน/งบประมาณทีมก่อน ซึ่งเอกสารต้นฉบับไม่ได้ระบุตัวเลข/กลไกไว้ชัด (ดูข้อ 1 ด้านล่าง) — **นี่คือเรื่องสำคัญที่สุดที่ต้องคุยกันก่อนเริ่มงานต่อ**
- **Phase 4 (คูปองรางวัลแชมป์)** — ก็ติดเรื่องเงินเหมือนกัน แต่ข้อมูลหอเกียรติยศที่ทำไว้แล้วพร้อมให้มาต่อได้ทันที
- **บอลโลก** (ส่วนหนึ่งของ Phase 3) — โครงสร้างต่างจากถ้วยทั่วไปมาก (จับคู่คน+สุ่มทีมชาติ) ตั้งใจแยกไว้ทำหลัง
- **Backup/restore อัตโนมัติ** (Phase 6 อีกส่วน) — ต้องสร้างทรัพยากร GCP ที่มีผลเรื่องค่าใช้จ่าย ไม่ควรทำโดยไม่ถามก่อน

**บั๊กจริงที่เจอและแก้ระหว่างทาง (ไม่ใช่แค่ทฤษฎี — เจอจากรัน test/ทดสอบจริงทั้งหมด):**
- Firestore Rules function ที่นิยามนอก `service cloud.firestore` ไม่เห็นตัวแปร `database` จาก path อัตโนมัติ (ต้องส่งเป็น parameter) — เจอจาก rules test ที่ fail แบบ error ไม่ชัดเจน
- Bracket generation ถ้า pad ทีมด้วย null แล้วจับคู่ตรงๆ ตามลำดับ จะเกิด "บายชนบาย" ทำให้รอบถัดไปค้างตลอดกาล — เจอ**ก่อน**รัน test ด้วยซ้ำ ตอนคิด logic เอง แก้ด้วยการแจกบายแยกคนละนัดก่อนจับคู่ทีมจริง
- หน้า log กิจกรรมแอดมิน subscribe ข้อมูลก่อนเช็ค role ทำให้ manager ที่หลงเข้ามาเปิด listener ที่โดน permission-denied ค้างไว้ — เจอจากทดสอบ manual จริง แก้แล้ว

**ไม่มีบั๊กไหนที่ยังคาราคาซังอยู่** — ทุกอันที่เจอแก้จบในคืนนี้แล้วทั้งหมด

---

## รายละเอียดแต่ละ Phase (เรียงตามลำดับที่ทำ)

### Phase 2.2 — ฉีกสัญญา (release) + ป้องกัน ping-pong ✅
- เพิ่ม `freeAgents` และ `releaseLog` collection ใต้ league
- `releasePlayer()`: ลบผู้เล่นจากทีม → สร้าง free agent (คง playerId เดิม) → log การฉีกสัญญาด้วย deterministic ID `${season}_${teamId}_${playerId}` (ถ้ามีอยู่แล้ว = บล็อกซ้ำ)
- Manager คุมทีมตัวเองฉีกสัญญาได้เอง (ไม่ต้องรอ admin) ผ่าน rule `managesTeam()` ที่ query `get()` เช็ค `managerUid`
- **บั๊กที่เจอจาก rules test แล้วแก้ได้จริง:** ฟังก์ชัน rules ที่นิยามไว้นอก `service cloud.firestore` (top-level) **ไม่เห็น** ตัวแปร `database` จาก path capture โดยอัตโนมัติ ต้องส่งเป็น parameter เข้าไปเอง (`managesTeam(database, leagueId, teamId)`) ไม่งั้น `get()` จะพังแบบ "Null value error" แบบไม่มี error message ที่ชัดเจน
- ทดสอบ manual ผ่าน emulator ครบ รวมถึงจำลอง ping-pong (ใช้ firebase-admin เขียนผู้เล่นตัวเดิม ID เดิมกลับเข้าทีม แล้วลองฉีกซ้ำ) → บล็อกถูกต้องด้วยข้อความ error ตรงเป้า

### Phase 2.3 — ขายตรงระหว่างทีม + โควตา ✅
- เพิ่ม `saleOffers` (เสนอราคา) และ `transfers` (ประวัติที่โอนสำเร็จแล้ว ใช้คำนวณโควตาสด) ใต้ league
- `src/features/leagues/quotas.ts` — pure function `canSell`/`canReceive` (โควตาขายผ่านประมูล ≤4, ขายรวม ≤5, รับเข้า ≤5 ต่อฤดูกาล ตามเอกสารข้อ 5.2 เป๊ะๆ) มี unit test ครบ
- Flow: ทีมใดทีมหนึ่ง (ผู้ขายหรือผู้ซื้อ) เสนอราคา → อีกฝ่ายกด "ยอมรับ" (แก้ได้แค่ field `status`) → **แอดมินกด "ปิดการขาย" เป็นคนย้ายผู้เล่นจริง** (เช็คโควตาก่อนย้ายเสมอ) → บันทึก `transfers` (คง playerId เดิมข้ามทีม เหมือน release)
- ทดสอบ manual ผ่าน emulator ครบ 3 ฝั่ง (ผู้ขาย เสนอ → ผู้ซื้อ ยอมรับ → แอดมิน ปิดการขาย) เช็ค Firestore ตรงว่าผู้เล่นย้ายทีมจริงพร้อม ID เดิม และ transfer record ถูกต้อง

### Phase 2.4 — ประมูล ✅
- เพิ่ม `auctionListings` collection: ทีมขายส่งเข้าคิว (`pending_approval`) → **แอดมินอนุมัติ** (ตอนนี้เองที่นับเข้าโควตาแล้ว แม้ยังไม่ปิดประมูล ตามเอกสารข้อ 5.2 ตรงตัว) → เปิดประมูล (`open`) manager ทีมอื่นประมูลได้เอง (ห้ามทีมขายประมูลนักเตะตัวเอง) → **แอดมินปิดประมูล** กำหนดผู้ชนะ/ย้ายผู้เล่นจริง
- ขยาย `quotas.ts`'s `canSell()` ให้รับ `pendingSameTypeCount` (จำนวนรายการประมูลที่เปิดอยู่แต่ยังไม่ปิดของทีมนั้น) มาบวกรวมกับ transfers ที่ปิดแล้ว
- **ใช้ Firestore `runTransaction` สำหรับการประมูล** (`placeBid`) กัน race condition ตอนมีคนประมูลพร้อมกันสองคน — ฝั่ง rules ก็เช็คซ้ำว่าราคาใหม่ต้องสูงกว่าราคาปัจจุบันจริง (defense in depth)
- **บั๊กที่เจอจาก rules test แล้วแก้ได้จริง (เป็น test ผมเขียนผิดเอง ไม่ใช่ rules ผิด):** เทสเดิมคาดหวังว่าประมูล "เท่าราคาเริ่ม" ต้องถูกปฏิเสธ แต่จริงๆ บิดแรกควรเปิดได้ที่ราคาตั้งต้นพอดี (>= ไม่ใช่ >) แก้ assertion ให้ตรงกับ business rule ที่ถูกต้อง
- ทดสอบ manual ผ่าน emulator ครบ: ส่งประมูล → อนุมัติ → ประมูลแข่งกัน → ปิดประมูล (มีผู้ชนะ) และเคส "ปิดประมูลไม่มีคนประมูลเลย" (`closed_no_winner`, ผู้เล่นไม่ถูกย้าย)
- **จุดที่ยังไม่ทำ:** ถ้าผู้ชนะประมูลเกินโควตารับเข้าตอนปิดประมูล ระบบแค่ throw error บล็อก ไม่ auto-fallback ไปเลือกอันดับสอง — แอดมินต้องจัดการเอง

### Phase 2.5/2.6 — Academy72 + คูปองส่วนลด: **หยุดไว้ก่อน ยังไม่ทำ** ⏸️
ข้ามไปทำ Phase 3 ต่อแทน เพราะทั้งสองอันพึ่งพาข้อมูลที่เอกสารต้นฉบับไม่ได้ระบุไว้ชัด:
- **Academy72**: ไม่รู้ "เปิดการ์ด" ได้ผู้เล่นสุ่มจากพูลไหน (ไม่มีฐานข้อมูลนักเตะกลาง), ไม่รู้ราคา, และ "บล็อกตอนส่ง Lineup ถ้าเกิน" ต้องมีระบบส่ง Lineup ก่อน (ยังไม่มี — Phase 1 เลื่อนไว้)
- **คูปองส่วนลด**: ใช้ลดราคาซื้อ แต่ยังไม่มีระบบเงิน/งบประมาณทีม และผูกกับ Academy72 ที่ก็สร้างไม่ได้ด้วยเหตุผลเดียวกัน

### Phase 3 — บอลถ้วย (แพ้คัดออก, ยังไม่รวมบอลโลก) ✅
- `src/features/cups/bracket.ts` เป็น pure function ล้วน (generateBracket, propagateWinner, updateCupMatchResult) แยกจาก Firestore — unit test 12 เคสรวม cascade หลายชั้น
- **แก้บั๊กจริงตั้งแต่ตอนเขียน (ก่อนรัน test ด้วยซ้ำ):** pad ทีมด้วย null แล้วจับคู่ตรงๆ ตามลำดับจะเกิด "บายชนบาย" (คู่ null-vs-null ตัดสินผลไม่ได้ ค้างตลอดกาล) แก้โดยแจกบายแยกคนละนัดก่อน แล้วค่อยจับคู่ทีมจริงที่เหลือ
- Bracket ทั้งชุด (ทุกรอบ) สร้างพร้อมกันตอน `createCup` เป็น placeholder รอผลไล่เติมผ่าน `propagateWinner`
- **cascade reset**: แก้ผลนัดที่ตัดสินไปแล้วให้ผู้ชนะเปลี่ยน จะไล่ล้างทุกรอบถัดไปที่เคยตัดสินจากผู้ชนะเดิมแบบ recursive (ไม่ใช่แค่ชั้นถัดไปชั้นเดียว) — เทสยืนยันด้วย bracket 8 ทีมที่ไล่ล้างทะลุ 2 ชั้น
- รายงาน/แก้ผลนัดบอลถ้วยเป็นสิทธิ์ **admin ทั้งหมดเสมอ** (ต่างจากนัดลีก) เพราะ propagate ผู้ชนะต้องเขียนข้าม document ของคู่ทีมอื่นที่ไม่เกี่ยวกับผู้รายงาน
- ทดสอบ manual ครบ: สร้างถ้วย 4 ทีม → รายงานผลรอบ 1 ทั้งคู่ → รายงานผลรอบชิง (สถานะ "จบแล้ว") → **แก้ผลรอบ 1 ย้อนหลัง → รอบชิงล้างกลับเป็นค่าว่าง + สถานะกลับเป็น "กำลังแข่ง" อัตโนมัติ**
- **ยังไม่ทำ (ตั้งใจ):** บอลโลก (จับคู่ manager สุ่ม + สุ่มทีมชาติ — ไม่มีรายชื่อทีมชาติในเอกสาร) และเงินรางวัลจบฤดูกาล (รอระบบเงิน)

### Phase 5 — หอเกียรติยศ ✅
- Top-level collection `hallOfFame/{entryId}` (ไม่ผูกกับลีกใดลีกหนึ่ง) เก็บ `managerUid`/`managerName`/`teamName` เป็น **snapshot ตายตัว** ตอนบันทึก ไม่ derive จากทีมปัจจุบันย้อนหลัง — ตรงตามที่เอกสารเตือนไว้เรื่อง "ผู้จัดการทีมที่ย้ายทีมเสียประวัติ"
- แก้ไม่ได้เลยแม้เป็น admin — ผิดให้ลบสร้างใหม่ กันไม่ให้ snapshot ถูกแก้แบบเงียบๆ
- ฟอร์มกรอกย้อนหลัง: เลือกลีก+ทีมจากดรอปดาวน์ (autofill) แต่พิมพ์เองได้เสมอ เผื่อทีมเก่าไม่มีในระบบแล้ว
- `isFirstChampionshipForManager()` — pure function detect แชมป์ครั้งแรกต่อประเภท มี unit test + ทดสอบ manual เห็น 🌟 ถูกจังหวะ
- **ยังไม่ทำ:** แจกคูปองจริงตอนแชมป์ครั้งแรก (แค่ detect+แสดงผล — รอระบบเงิน) และไม่มี auto-capture ตอนจบฤดูกาล (ตั้งใจ เป็นระบบกรอกมือทั้งหมดตามที่เอกสารเน้นเรื่อง backfill)

### Phase 6 (บางส่วน) — Log กิจกรรมแอดมิน ✅
- Top-level collection `adminActivityLog/{logId}` — อ่านได้เฉพาะ admin, เขียนได้เฉพาะ admin และต้อง log การกระทำของตัวเองเท่านั้น (`actorUid == request.auth.uid`), แก้/ลบไม่ได้เด็ดขาด
- ใช้ `serverTimestamp()` เสมอ + ผูก log write เข้า batch เดียวกับ action หลักเสมอ (ไม่ใช่ write แยก)
- Retrofit เข้า admin action ที่กระทบข้อมูลมากที่สุดก่อน: `create/delete_league`, `end/start_season`, `set_team_forfeited`, `complete_sale`, `approve/reject/close_auction`, `create_cup`, `report/edit_cup_match_result`, `add/remove_hall_of_fame_entry`
- **ป้องกันไว้ตั้งแต่ตอนออกแบบ:** `writeBatch` เป็น atomic ถ้า log write ถูก rules ปฏิเสธจะฉุด action หลักพังไปด้วย — `buildCurrentAdminLogWrite` จึงคืนฟังก์ชัน no-op เงียบๆถ้าไม่มี `auth.currentUser` แทนที่จะเสี่ยงพังของจริง
- **เจอบั๊กเล็กจากทดสอบจริง แก้แล้ว:** หน้า log เดิม subscribe ก่อนเช็ค role ทำให้ manager ที่หลงเข้ามาเปิด listener ค้าง (permission-denied) แก้ให้เช็ค role ก่อน subscribe
- รันเทสทั้งชุดซ้ำหลัง retrofit (62 rules + 38 unit) ผ่านหมด ยืนยันไม่มี regression
- **ยังไม่ทำ (ตั้งใจ):** ยังไม่ retrofit action ของ manager เอง (release, สร้าง listing/offer, ประมูล) และไม่ได้แตะ backup/restore (ต้องสร้างทรัพยากร GCP ที่มีผลค่าใช้จ่าย ควรถามก่อน)

---

## ข้อสมมติ/จุดที่ต้องตัดสินใจเอง (ทบทวนตอนเช้า — เรียงตามความสำคัญ)

1. **[สำคัญที่สุด] เรื่องเงิน/งบประมาณทีมยังไม่มีในระบบเลย** — เอกสารพูดถึง "เงินรางวัล" และ "ทีมใช้เงินไปฉีกสัญญา/ซื้อนักเตะ" แต่ไม่ระบุตัวเลขเริ่มต้น/สูตรราคา ตอนนี้ฉีกสัญญา/ซื้อขายไม่มีผลต่อเงินใดๆ (ราคาขายเป็นเลขที่สองทีมตกลงกันเอง เก็บไว้ในประวัติเฉยๆ) **ต้องคุยกันจริงจังว่าจะมีระบบเงินไหม เริ่มต้นเท่าไหร่** ก่อนทำ Academy72/คูปอง/คูปองรางวัลแชมป์ต่อ
2. **ทำไมขายย่อย/ประมูล/บอลถ้วยต้องผ่านแอดมินกดจริงเสมอ (ไม่ให้ manager ทำเองจบในตัว):** ติด Firestore Security Rules — การย้ายข้อมูลข้ามทีม/ข้ามคู่ต้องเขียนหลาย document ที่ manager คนเดียวไม่มีสิทธิ์ครบทั้งสองฝั่งอย่างปลอดภัย ถ้าอยากให้ manager ทำเองได้ทั้งหมดต้องเพิ่ม Cloud Function (มีผลเรื่องงบ hosting) — ตัดสินใจยึด pattern เดิมจาก Phase 0 ที่ไม่ใช้ Cloud Function
3. **สถานะลีกที่ใช้เปิดตลาดจริงๆคือ "กำลังแข่งขัน" (in_season) ไม่ใช่ "ตลาดเปิด" (transfer_window)** — ตามตัวหนังสือเอกสารข้อ 5.2 เป๊ะๆ (ชื่อ status สลับความหมายจากที่คนทั่วไปจะเดา) ผมทำตามเอกสารตรงๆ ไม่ได้เดาเอง แต่อยากให้ double-check เพราะถ้าผิดกระทบทุก sub-phase ตลาด
4. **ใครฉีกสัญญาได้:** manager เจ้าของทีมทำเองได้เลย ไม่ต้องรอ admin (ต่างจากประมูลที่เอกสารบอกชัดว่าต้อง admin อนุมัติ) เพราะเอกสารไม่ได้ระบุว่าฉีกสัญญาต้องขออนุมัติ
5. **ยังไม่มีระบบเลือก/สุ่มลำดับทีมก่อนสร้าง bracket ถ้วย** — แอดมินติ๊กเลือกทีมแล้วจับคู่ตามลำดับที่ติ๊ก ไม่สุ่ม ไม่ seed ตามอันดับ
6. **หอเกียรติยศเป็นระบบกรอกมือทั้งหมด ไม่ auto-capture ตอนจบฤดูกาล/ปิดถ้วย** — ตีความจากที่เอกสารเน้นเรื่อง "กรอกย้อนหลัง" ถ้าอยากให้ auto-capture ต้องเพิ่ม field "tier" (ลีกสูงสุด/รอง) ให้ League ก่อน (ตอนนี้ยังไม่มี)
7. **Log กิจกรรมแอดมินยังไม่ครบทุก action** — เลือกทำจุดที่แอดมินกดจริงและกระทบข้อมูลวงกว้างก่อน ถ้าต้องการครบทุกจุด (รวม manager self-service ด้วยหรือไม่) บอกได้เลย

---

## วิธีกลับมาทำงานต่อ

โค้ดทั้งหมด push ขึ้น `main` แล้ว (commit ล่าสุด `bd78736`) เว็บจริง deploy อัตโนมัติเรียบร้อย ถ้าจะทำต่อ:
- **ตอบข้อ 1 (เงิน) ก่อน** — ถึงจะไปต่อ Phase 2.5/2.6/Phase 4 ได้จริง
- ที่เหลือ (ข้อ 2-7) เป็นแค่ยืนยัน/ปรับได้ ไม่ block งานถัดไป
